<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Service</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css">

    <style>
        body {
            background: linear-gradient(135deg, #1f1c2c, #928dab);
            min-height: 100vh;
            color: #f8f9fa;
            font-family: 'Poppins', sans-serif;
            padding-top: 60px;
        }

        .card-custom {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 40px;
            backdrop-filter: blur(10px);
            box-shadow: 0 12px 35px rgba(0, 0, 0, 0.4);
            border: 1px solid rgba(255, 255, 255, 0.2);
            animation: fadeIn 1.1s ease;
        }

        h1 {
            font-weight: 700;
            text-shadow: 0 0 15px rgba(255, 255, 255, 0.7);
        }

        .para {
            font-size: 1.1rem;
            opacity: 0.9;
        }

        .btn-back {
            display: inline-block;
            margin-top: 20px;
            background: #00e0ff;
            color: #000;
            padding: 12px 28px;
            border-radius: 40px;
            text-decoration: none;
            font-weight: 600;
            transition: 0.3s;
        }

        .btn-back:hover {
            transform: translateY(-3px);
            background: #3ff2ff;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>

<body>

    <div class="container d-flex justify-content-center">
        <div class="card-custom text-center" style="max-width: 600px; width: 100%;">

            <h1 class="mb-4">⚙️ Service Page</h1>
            <p class="para">
                Experience a clean, modern and stylish UI.
                This service page is fully responsive and visually enhanced.
            </p>

            <a href="/" class="btn-back">⬅ Back Home</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>